{
  "name": "weixinmp-repo",
  "private": true,
  "description": "Build and release tooling for the weixinmp CLI repository.",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "node --test ./npm/tests/*.test.cjs",
    "package-npm": "node ./scripts/npm/prepare-packages.mjs",
    "pack-npm": "node ./scripts/npm/prepare-packages.mjs --pack",
    "publish-npm": "node ./scripts/npm/publish-packages.mjs",
    "verify-npm-install": "node ./scripts/npm/verify-install.mjs"
  }
}
